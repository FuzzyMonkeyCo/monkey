{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "additionalProperties": false,
    "properties": {
        "cmd": {"enum": ["done"]},
        "v": {"enum": [1]},
        "failure": {"type": "boolean"}
    },
    "required": [
        "v",
        "cmd",
        "failure"
    ],
    "type": "object"
}
